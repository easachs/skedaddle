<% if items&.any? %>
  <%= tag.div id: category.downcase do %>
    <%= tag.h3 category.capitalize, class: 'font-bold p-2 mb-2' %>
    <%= render Sub::BusinessComponent.with_collection(items, saved:) %>
  <% end %>
<% end %>
