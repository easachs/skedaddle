<%= render Core::ModalComponent.new trigger: (link_to 'Delete', '#', class: trigger_class) do %>
  <%= tag.div 'Are you sure you want to delete this item?', class: 'text-center' %>
  <%= tag.div class: 'flex justify-between mx-8 mt-2' do %>
    <%= button_to 'Delete', removal_path, method: :delete,
                  class: 'btn red-btn m-2' %>
    <%= link_to 'Cancel', '#', data: { action: 'modal#close' },
                class: 'btn gray-btn m-2' %>
  <% end %>
<% end %>
